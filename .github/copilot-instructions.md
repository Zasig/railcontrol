# Copilot Instructions

## General Guidelines
- Do not run 'build' or invoke the build system in the workspace unless the user explicitly requests it. Preference applies to automated build/run_build calls.